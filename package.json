{
  "name": "london-live-2d",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install --include=dev",
    "start": "cd backend && npm run start"
  },
  "engines": {
    "node": ">=20"
  }
}
