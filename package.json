{
  "name": "london-live-2d",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build && npm --prefix backend ci --include=dev",
    "start": "npm --prefix backend run start"
  },
  "engines": {
    "node": ">=20"
  }
}
